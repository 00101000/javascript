// Write a JavaScript program to show all equivalent elements in two given arrays of integers.
function checkOcccurance(arrOne, arrTwo) {
    arrOne.forEach(element => {
        if (arrTwo.indexOf(element) != -1) console.log(`element`)  
    });
    return
}


checkOcccurance([1, 2, 3, 4], [3, 4, 5])